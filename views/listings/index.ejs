<%- layout("/layouts/boilerplate.ejs") %>

<!-- Category Filters & Tax Toggle -->
<div id="filters">
    <a href="/listings?category=Trending" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Trending" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-fire"></i></div>
        <p>Trending</p>
    </a>
    <a href="/listings?category=Rooms" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Rooms" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-bed"></i></div>
        <p>Rooms</p>
    </a>
    <a href="/listings?category=Iconic Cities" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Iconic Cities" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-mountain-city"></i></div>
        <p>Iconic Cities</p>
    </a>
    <a href="/listings?category=Mountains" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Mountains" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-mountain"></i></div>
        <p>Mountains</p>
    </a>
    <a href="/listings?category=Castles" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Castles" ? "active-filter" : "" %>">
        <div><i class="fa-brands fa-fort-awesome"></i></div>
        <p>Castles</p>
    </a>
    <a href="/listings?category=Amazing Pools" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Amazing Pools" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-person-swimming"></i></div>
        <p>Amazing Pools</p>
    </a>
    <a href="/listings?category=Camping" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Camping" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-campground"></i></div>
        <p>Camping</p>
    </a>
    <a href="/listings?category=Farms" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Farms" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-cow"></i></div>
        <p>Farms</p>
    </a>
    <a href="/listings?category=Arctic" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Arctic" ? "active-filter" : "" %>">
        <div><i class="fa-regular fa-snowflake"></i></div>
        <p>Arctic</p>
    </a>
    <a href="/listings?category=Domes" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Domes" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-igloo"></i></div>
        <p>Domes</p>
    </a>
    <a href="/listings?category=Boats" class="filter <%= typeof selectedCategory !== "undefined" && selectedCategory === "Boats" ? "active-filter" : "" %>">
        <div><i class="fa-solid fa-ship"></i></div>
        <p>Boats</p>
    </a>

    <!-- Tax Switch Toggle -->
    <div class="tax-toggle">
        <div class="form-check-reverse form-switch">
            <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault" style="cursor: pointer;">
            <label class="form-check-label" for="flexSwitchCheckDefault" style="cursor: pointer; font-size: 0.85rem; font-weight: 600;">Display total after taxes</label>
        </div>
    </div>
</div>

<% if (data.length === 0) { %>
    <div class="alert alert-warning mt-4 text-center" role="alert">
        <h5>No listings found matching your search.</h5>
        <p class="mb-0"><a href="/listings" class="alert-link">Click here to view all available listings</a></p>
    </div>
<% } %>

<!-- Listings Cards Grid -->
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
    <% for(let listing of data) { %>
        <div class="col listing-card-wrapper mb-4">
            <!-- Wishlist Button -->
            <button class="wishlist-btn" onclick="toggleWishlist(event, this, '<%= listing._id %>')" title="Save to wishlist">
                <i class="fa-regular fa-heart"></i>
            </button>

            <a class="card-link text-decoration-none" href="/listings/<%= listing._id %>">
                <div class="card listing-card h-100">
                    <img src="<%= listing.image.url %>" class="card-img-top" alt="Listing image" style="height: 20rem; border-radius: 1rem; object-fit: cover;">
                    <div class="card-img-overlay"></div>
                    <div class="card-body pt-2">
                        <p class="card-text text-dark">
                            <b class="fs-6"><%= listing.title %></b> <br>
                            <span class="text-muted fs-6"><i class="fa-solid fa-location-dot text-danger"></i> <%= listing.location %>, <%= listing.country %></span> <br>
                            <span class="fw-bold mt-1 d-inline-block">&#8377; <%= Number(listing.price).toLocaleString("en-IN") %></span> <span class="text-muted">/ night</span>
                            <i class="tax-info"> &nbsp; &nbsp;+18% GST (&#8377; <%= Number(listing.price * 1.18).toLocaleString("en-IN", {maximumFractionDigits: 0}) %> total)</i>
                        </p>
                    </div>
                </div>
            </a>
        </div>
    <% } %>
</div>

<!-- Real-Time Tax Toggle & Wishlist Script -->
<script>
    const taxSwitch = document.getElementById("flexSwitchCheckDefault");
    if (taxSwitch) {
        taxSwitch.addEventListener("click", () => {
            const taxInfo = document.getElementsByClassName("tax-info");
            for (let info of taxInfo) {
                if (info.style.display !== "inline") {
                    info.style.display = "inline";
                } else {
                    info.style.display = "none";
                }
            }
        });
    }

    function toggleWishlist(event, btn, listingId) {
        event.stopPropagation();
        event.preventDefault();
        btn.classList.toggle("active-heart");
        const icon = btn.querySelector("i");
        if (btn.classList.contains("active-heart")) {
            icon.classList.remove("fa-regular");
            icon.classList.add("fa-solid");
            localStorage.setItem("wishlist_" + listingId, "true");
        } else {
            icon.classList.remove("fa-solid");
            icon.classList.add("fa-regular");
            localStorage.removeItem("wishlist_" + listingId);
        }
    }

    // Load saved wishlist hearts
    document.addEventListener("DOMContentLoaded", () => {
        document.querySelectorAll(".wishlist-btn").forEach(btn => {
            const onclickAttr = btn.getAttribute("onclick");
            const match = onclickAttr.match(/'([^']+)')$/);
            if (match && localStorage.getItem("wishlist_" + match[1])) {
                btn.classList.add("active-heart");
                const icon = btn.querySelector("i");
                icon.classList.remove("fa-regular");
                icon.classList.add("fa-solid");
            }
        });
    });
</script>
